 (cd "$(git rev-parse --show-toplevel)" && git apply --3way <<'EOF' 
diff --git a/src/Gamefiles/Systems/EditorUISystem.cpp b/src/Gamefiles/Systems/EditorUISystem.cpp
index 031eca3c9bdcc9800e73e5f15db3c8778b7dc583..d4117e391c196668dca6b6cc624ae4cba06ccf5c 100644
--- a/src/Gamefiles/Systems/EditorUISystem.cpp
+++ b/src/Gamefiles/Systems/EditorUISystem.cpp
@@ -887,50 +887,60 @@ void EditorUISystem::update (Registry & registry, float deltatime) {
                     float dx = dir_position.x - orig_position.x;
                     float dy = dir_position.y - orig_position.y;
 
                     // Calculate the angle in radians between the vector and the positive X-axis
                     float angle_rad = atan2(dy, dx);
 
                     // Convert radians to degrees
                     float angle_deg = angle_rad * 180.0f / 3.14159265358979323846f;
 
                     // Optional: Normalize angle to [0, 360) degrees
                     if (angle_deg < 0) {
                         angle_deg += 360.0f;
                     }
 
                     return angle_deg;
                 };
 
 
                 // Draw canvas base
                 draw->AddRectFilled(canvasPos, ImVec2(canvasPos.x+canvasSize.x, canvasPos.y+canvasSize.y), IM_COL32(20,20,30,255));
 
                 // Get mouse position
                 ImVec2 mpos = ImGui::GetIO().MousePos;
 
 
+                static bool showOnionPrevFrame = false;
+                static bool showOnionAnchor = false;
+
+                ImGui::AlignTextToFramePadding();
+                ImGui::Text("Frame: %d", selected_anim_frame);
+                ImGui::SameLine();
+                ImGui::Checkbox("Onion Prev", &showOnionPrevFrame);
+                ImGui::SameLine();
+                ImGui::Checkbox("Show Anchor", &showOnionAnchor);
+
                 // DRAW JOINTS and IMAGE TEXTURES in LOOP
                 // **************************************
                 
                 // ANCHOR FRAME: DRAW
                 if (selected_anim_frame == -1) {
 
                     for (int i = 0; i < (*avatar_selected).default_texturing.joints.size(); i++) {
 
                         int idx = get_idx_from_jlayer_id((*avatar_selected), i);
 
                         auto& j = (*avatar_selected).default_frame.joints[idx];
                         auto& j_text = (*avatar_selected).default_texturing.joints[idx];
 
                         ImVec2 a = WorldToScreen(ImVec2(j.origin.x, j.origin.y));
                         ImVec2 b = WorldToScreen(ImVec2(j.origin.x + j.direction.x,
                                                     j.origin.y + j.direction.y));
 
                         float dist = DistancePointToSegment(mpos, a, b);
 
                         bool hovered = dist < 8.0f;
                         bool selected = (i == jointselected);
 
                         ImU32 color;
 
                         if (selected) {
@@ -996,50 +1006,103 @@ void EditorUISystem::update (Registry & registry, float deltatime) {
                                 rotated[1],
                                 rotated[2],
                                 rotated[3],
                                 ImVec2(uv0.x, uv0.y),
                                 ImVec2(uv1.x, uv0.y),
                                 ImVec2(uv1.x, uv1.y),
                                 ImVec2(uv0.x, uv1.y)
                             );
                         }
 
                         // CLICK SELECT
                         if (hovered && ImGui::IsMouseClicked(ImGuiMouseButton_Left)) {
                             jointselected = idx;
                         }
 
                 
                     }
 
                 } else {
 
                     
 
 
 
                     // NORMAL FRAME: DRAW
+                    if (showOnionPrevFrame && selected_anim_frame > 0 && selected_anim_frame < (int)animations[anim_selected].frames.size()) {
+                        const KeyAnimFrame& prevFrame = animations[anim_selected].frames[selected_anim_frame - 1];
+                        for (int i = 0; i < (*avatar_selected).default_texturing.joints.size(); i++) {
+                            int idx = get_idx_from_jlayer_id((*avatar_selected), i);
+                            auto& anchor_j = (*avatar_selected).default_frame.joints[idx];
+                            auto& prev_j = prevFrame.joints[idx];
+                            auto& j_text = (*avatar_selected).default_texturing.joints[idx];
+
+                            float prev_dir_x = RotNewPositionVec({anchor_j.origin.x + prev_j.origin.x, anchor_j.origin.y + prev_j.origin.y}, anchor_j.direction, prev_j.rotation).x;
+                            float prev_dir_y = RotNewPositionVec({anchor_j.origin.x + prev_j.origin.x, anchor_j.origin.y + prev_j.origin.y}, anchor_j.direction, prev_j.rotation).y;
+
+                            ImVec2 aPrev = WorldToScreen(ImVec2(anchor_j.origin.x + prev_j.origin.x, anchor_j.origin.y + prev_j.origin.y));
+                            ImVec2 bPrev = WorldToScreen(ImVec2(prev_dir_x, prev_dir_y));
+
+                            draw->AddLine(aPrev, bPrev, IM_COL32(160, 180, 255, 110), 2.0f);
+
+                            if (j_text.texture != nullptr) {
+                                ImVec2 center = WorldToScreen(ImVec2(anchor_j.origin.x + prev_j.origin.x, anchor_j.origin.y + prev_j.origin.y));
+                                float width  = j_text.texture->width  * j_text.scale.x * local_canvas_zoom;
+                                float height = j_text.texture->height * j_text.scale.y * local_canvas_zoom;
+                                Vec2 newDir = RotNewDirectionVec(anchor_j.direction, prev_j.rotation);
+                                ImVec2 dir = { newDir.x, newDir.y };
+                                float baseAngle = atan2f(dir.y, dir.x);
+                                float angle = baseAngle + j_text.rotation;
+                                float cosA = cosf(angle);
+                                float sinA = sinf(angle);
+                                ImVec2 offset = { j_text.offset.x * local_canvas_zoom, j_text.offset.y * local_canvas_zoom };
+                                ImVec2 half = { width * 0.5f, height * 0.5f };
+                                ImVec2 corners[4] = {{-half.x, -half.y},{ half.x, -half.y},{ half.x,  half.y},{-half.x,  half.y}};
+                                ImVec2 rotated[4];
+                                for (int k = 0; k < 4; k++) {
+                                    float x = corners[k].x + offset.x;
+                                    float y = corners[k].y + offset.y;
+                                    rotated[k].x = center.x + (x * cosA - y * sinA);
+                                    rotated[k].y = center.y + (x * sinA + y * cosA);
+                                }
+                                ImVec2 uv0 = { j_text.crop_min.x, j_text.crop_min.y };
+                                ImVec2 uv1 = { j_text.crop_max.x, j_text.crop_max.y };
+                                draw->AddImageQuad((ImTextureID)(intptr_t)j_text.texture->id, rotated[0], rotated[1], rotated[2], rotated[3], ImVec2(uv0.x, uv0.y), ImVec2(uv1.x, uv0.y), ImVec2(uv1.x, uv1.y), ImVec2(uv0.x, uv1.y), IM_COL32(255,255,255,95));
+                            }
+                        }
+                    }
+
+                    if (showOnionAnchor) {
+                        for (int i = 0; i < (*avatar_selected).default_texturing.joints.size(); i++) {
+                            int idx = get_idx_from_jlayer_id((*avatar_selected), i);
+                            auto& j = (*avatar_selected).default_frame.joints[idx];
+                            ImVec2 a = WorldToScreen(ImVec2(j.origin.x, j.origin.y));
+                            ImVec2 b = WorldToScreen(ImVec2(j.origin.x + j.direction.x, j.origin.y + j.direction.y));
+                            draw->AddLine(a, b, IM_COL32(80, 220, 120, 110), 2.0f);
+                        }
+                    }
+
                     for (int i = 0; i < (*avatar_selected).default_texturing.joints.size(); i++) {
 
                         int idx = get_idx_from_jlayer_id((*avatar_selected), i);
 
                         auto& anchor_j = (*avatar_selected).default_frame.joints[idx];
                         auto& anim_j = animations[anim_selected].frames[selected_anim_frame].joints[idx];
 
                         auto& j_text = (*avatar_selected).default_texturing.joints[idx];
 
                       
                         float direction_point_x = 0.0f;
                         float direction_point_y = 0.0f;
 
                         direction_point_x = RotNewPositionVec({anchor_j.origin.x + anim_j.origin.x, anchor_j.origin.y + anim_j.origin.y}, anchor_j.direction, anim_j.rotation).x;
                         direction_point_y = RotNewPositionVec({anchor_j.origin.x + anim_j.origin.x, anchor_j.origin.y + anim_j.origin.y}, anchor_j.direction, anim_j.rotation).y;
 
                         ImVec2 a = WorldToScreen(ImVec2(anchor_j.origin.x + anim_j.origin.x, anchor_j.origin.y + anim_j.origin.y));
                         ImVec2 b = WorldToScreen(ImVec2(direction_point_x, direction_point_y));
 
                         float dist = DistancePointToSegment(mpos, a, b);
 
                         bool hovered = dist < 8.0f;
                         bool selected = (i == jointselected);
 
                         ImU32 color;
@@ -1720,111 +1783,103 @@ void EditorUISystem::update (Registry & registry, float deltatime) {
 
                             // ============================================================
                             // TIMELINE PANEL SPACE
                             // ============================================================
 
                             ImVec2 p0 = ImGui::GetCursorScreenPos();
                             ImVec2 avail = ImGui::GetContentRegionAvail();
 
                             ImDrawList* dl = ImGui::GetWindowDrawList();
 
                             dl->AddRectFilled(
                                 p0,
                                 ImVec2(p0.x + avail.x, p0.y + avail.y),
                                 IM_COL32(22,22,22,255)
                             );
 
                             // ============================================================
                             // TIMELINE SETTINGS
                             // ============================================================
 
                             static int totalFrames = 300;
 
                             static int currentFrame = 40;
 
                             // THIS is the important part
-                            static float timelineScroll = 0.0f;
-
                             // zoom level for time spacing
                             static float pixelsPerFrame = 18.0f;
 
                             // ============================================================
                             // SCROLL + ZOOM
                             // ============================================================
 
                             ImVec2 mouse = ImGui::GetIO().MousePos;
 
                             bool hovered =
                                 mouse.x >= p0.x &&
                                 mouse.x <= p0.x + avail.x &&
                                 mouse.y >= p0.y &&
                                 mouse.y <= p0.y + avail.y;
 
                             if (hovered) {
 
                                 // vertical scroll through time
-                                timelineScroll -= ImGui::GetIO().MouseWheel * 60.0f;
-
-                                if (timelineScroll < 0.0f)
-                                    timelineScroll = 0.0f;
-
                                 // CTRL + wheel = zoom
                                 if (ImGui::GetIO().KeyCtrl) {
 
                                     pixelsPerFrame += ImGui::GetIO().MouseWheel * 2.0f;
 
                                     if (pixelsPerFrame < 6.0f)
                                         pixelsPerFrame = 6.0f;
 
                                     if (pixelsPerFrame > 60.0f)
                                         pixelsPerFrame = 60.0f;
                                 }
                             }
 
                             // ============================================================
                             // RAIL
                             // ============================================================
 
                             float railX = p0.x + 80.0f;
 
                             dl->AddLine(
                                 ImVec2(railX, p0.y),
                                 ImVec2(railX, p0.y + avail.y),
                                 IM_COL32(90,90,90,255),
                                 3.0f
                             );
 
                             // ============================================================
                             // DRAW FRAMES
                             // ============================================================
 
                             for (int frame = 0; frame <= totalFrames; frame++) {
 
                                 float y =
                                     p0.y +
-                                    (frame * pixelsPerFrame) -
-                                    timelineScroll;
+                                    (frame * pixelsPerFrame);
 
                                 // cull invisible frames
                                 if (y < p0.y - 40.0f)
                                     continue;
 
                                 if (y > p0.y + avail.y + 40.0f)
                                     continue;
 
                                 bool major = (frame % 10 == 0);
 
                                 float tick =
                                     major ? 26.0f : 14.0f;
 
                                 dl->AddLine(
                                     ImVec2(railX - tick * 0.5f, y),
                                     ImVec2(railX + tick * 0.5f, y),
                                     IM_COL32(170,170,170,255),
                                     major ? 2.0f : 1.0f
                                 );
 
                                 if (major) {
 
                                     dl->AddText(
                                         ImVec2(p0.x + 10.0f, y - 8.0f),
                                         IM_COL32(220,220,220,255),
@@ -1835,105 +1890,103 @@ void EditorUISystem::update (Registry & registry, float deltatime) {
 
                             // ============================================================
                             // EXAMPLE FLAGS
                             // ============================================================
 
                             struct TimelineFlag {
                                 int frame;
                                 ImU32 color;
                             };
 
                             std::vector<TimelineFlag> flags;
 
                             int i = 0;
 
                             for (const KeyAnimFrame & fr : animations[anim_selected].frames) {
 
                                 flags.push_back({(i * 10), IM_COL32 ( ( (255 + (i * 30)) % 255) , ( (120) % 255), ( (180 + (i * 30)) % 255), 255 ) });
                                 i++;
 
                             }
 
                             for (const auto& flag : flags) {
 
                                 float y =
                                     p0.y +
-                                    (flag.frame * pixelsPerFrame) -
-                                    timelineScroll;
+                                    (flag.frame * pixelsPerFrame);
 
                                 if (y < p0.y - 20.0f)
                                     continue;
 
                                 if (y > p0.y + avail.y + 20.0f)
                                     continue;
 
                                 dl->AddLine(
                                     ImVec2(railX, y),
                                     ImVec2(railX + 14.0f, y),
                                     flag.color,
                                     2.0f
                                 );
 
                                 dl->AddRectFilled(
                                     ImVec2(railX + 14.0f, y - 5.0f),
                                     ImVec2(railX + 34.0f, y + 5.0f),
                                     flag.color,
                                     2.0f
                                 );
                             }
 
                             // ============================================================
                             // SCRUBBER
                             // ============================================================
 
                             float scrubY =
                                 p0.y +
-                                (currentFrame * pixelsPerFrame) -
-                                timelineScroll;
+                                (currentFrame * pixelsPerFrame);
 
                             bool hoverScrubber =
                                 mouse.x >= p0.x &&
                                 mouse.x <= p0.x + avail.x &&
                                 mouse.y >= scrubY - 12.0f &&
                                 mouse.y <= scrubY + 12.0f;
 
                             static bool draggingScrubber = false;
 
                             if (hoverScrubber &&
                                 ImGui::IsMouseClicked(ImGuiMouseButton_Left))
                             {
                                 draggingScrubber = true;
                             }
 
                             if (!ImGui::IsMouseDown(ImGuiMouseButton_Left)) {
                                 draggingScrubber = false;
                             }
 
                             if (draggingScrubber) {
 
                                 float local =
-                                    mouse.y - p0.y + timelineScroll;
+                                    mouse.y - p0.y;
 
                                 currentFrame =
                                     (int)round(local / pixelsPerFrame);
 
                                 currentFrame =
                                     std::clamp(currentFrame, 0, totalFrames);
                             }
 
                             // scrubber line
                             dl->AddLine(
                                 ImVec2(p0.x, scrubY),
                                 ImVec2(p0.x + avail.x, scrubY),
                                 IM_COL32(255,40,40,255),
                                 3.0f
                             );
 
                             // scrubber triangle
                             dl->AddTriangleFilled(
                                 ImVec2(railX - 18.0f, scrubY),
                                 ImVec2(railX - 2.0f, scrubY - 10.0f),
                                 ImVec2(railX - 2.0f, scrubY + 10.0f),
                                 IM_COL32(255,50,50,255)
                             );
 
                             // current frame label
 
EOF
)